Etapas para configurar o deploy seguro no Azure
1. Gerar o perfil de publicação (publish profile)
No portal do Azure:
- Vá até o seu recurso do App Service
- No menu lateral, clique em "Perfil de publicação"
- Baixe o arquivo .PublishSettings
- Copie o conteúdo XML do arquivo

2. Adicionar o publish profile como secret no GitHub
No seu repositório:
- Vá em Settings > Secrets and variables > Actions
- Crie um novo secret chamado: AZURE_WEBAPP_PUBLISH_PROFILE
- Cole o conteúdo do XML do publish profile

3. Criar o workflow de deploy
Crie o arquivo .github/workflows/deploy.yml com o seguinte conteúdo:


=============================
Opção 1: Deploy para EC2 via SSH
Essa é a forma mais direta e parecida com o VPS:
✅ Pré-requisitos
- Uma instância EC2 rodando Linux com Python instalado
- Chave SSH configurada
- Git instalado na EC2
- Seu repositório no GitHub
🔐 Segurança
- Gere uma chave SSH e adicione a pública na EC2 (~/.ssh/authorized_keys)
- Adicione a chave privada como secret no GitHub (AWS_SSH_KEY)
- Use appleboy/ssh-action para copiar e executar comandos remotamente

Opção 2: Deploy com AWS CodeDeploy + IAM + OIDC
Essa é mais robusta e segura, ideal para ambientes profissionais.
✅ Pré-requisitos
- Criar uma aplicação no AWS CodeDeploy
- Configurar funções IAM para EC2 e CodeDeploy
- Usar OIDC (OpenID Connect) para autenticação segura entre GitHub e AWS
🔐 Segurança com OIDC
- GitHub assume uma role temporária via token seguro
- Sem necessidade de armazenar credenciais estáticas
- Permissões finamente controladas via IA
